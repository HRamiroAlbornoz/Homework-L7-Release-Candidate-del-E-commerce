// Placeholder: la lógica real de checkout está fuera del alcance de este
// homework de autenticación. Esta página solo sirve como superficie para
// probar ProtectedRoute (Paso 9): requiere sesión iniciada para acceder.
export function CheckoutPage() {
  return (
    <div className="page">
      <h1>Checkout</h1>
      <p>Esta sección todavía no tiene lógica de checkout implementada.</p>
    </div>
  );
}
