// Placeholder: la lógica real del carrito está fuera del alcance de este
// homework de autenticación. Esta página solo sirve como superficie para
// probar ProtectedRoute (Paso 9): requiere sesión iniciada para acceder.
export function CartPage() {
  return (
    <div className="page">
      <h1>Carrito</h1>
      <p>Esta sección todavía no tiene lógica de carrito implementada.</p>
    </div>
  );
}
